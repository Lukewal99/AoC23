FirstColumn = Advent(:,1);
FirstRow = Advent(1,:);

numOfRows = length(FirstColumn);
numOfCols = length(FirstRow);

for i = 1:numOfRows
    if(contains(Advent(i,:),"#"))
        
    else
        
    end
end